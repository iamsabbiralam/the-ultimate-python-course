# replace the double space from problem 3 with single space
name = "Python is a  programming  language"
print(name.replace("  ", " "))
print(name) # strings are immutable which means they cannot change the by running functions on them

# find the first occurrence of double spaces in the string. If not found, return -1
